apiVersion: v1
kind: Service
metadata:
  name: state-ui
  namespace: state-system
  labels:
    app.kubernetes.io/name: state-ui
spec:
  type: ClusterIP # INTERNAL ONLY — expose via internal ingress/SSO proxy overlay only
  selector:
    app.kubernetes.io/name: state-ui
  ports:
    - name: http
      port: 8080
      targetPort: http
