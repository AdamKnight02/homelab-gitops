---
# AWS Trust Domain Patch
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: spire-server
  namespace: spire
spec:
  template:
    spec:
      containers:
      - name: spire-server
        env:
        - name: SPIRE_TRUST_DOMAIN
          value: "pki-aws.local"
