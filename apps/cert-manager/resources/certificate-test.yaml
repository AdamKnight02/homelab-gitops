apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homelab-test
  namespace: cert-manager
spec:
  secretName: homelab-test-tls
  issuerRef:
    name: homelab-selfsigned
    kind: ClusterIssuer
  dnsNames:
    - test.homelab.local
