apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Note: k3s-pod-watcher.service and k3s-pod-watcher.sh are systemd/shell files
# They are deployed via VM configuration, not Kubernetes
# This kustomization is intentionally empty for K8s validation

resources: []
