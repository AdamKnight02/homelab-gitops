---
# Default Deny NetworkPolicy for PKI namespace
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: pki
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
