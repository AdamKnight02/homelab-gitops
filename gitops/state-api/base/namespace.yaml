apiVersion: v1
kind: Namespace
metadata:
  name: state-system
  labels:
    app.kubernetes.io/name: state-system
    app.kubernetes.io/part-of: pki-platform
    # Internal-only namespace — no public ingress allowed by policy
    networking.pki.platform.io/exposure: internal
