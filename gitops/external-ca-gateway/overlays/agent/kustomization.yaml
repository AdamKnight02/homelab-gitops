apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pki

resources:
  - ../../base
  - config.yaml

images:
  - name: registry/external-ca-gateway
    newTag: agent-client-1.4.2
